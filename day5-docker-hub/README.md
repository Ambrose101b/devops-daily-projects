# Day 5: Docker Hub & Image Registry

I successfully moved my containerized application from a local environment to the Public Cloud.

### Achievements:
- Authenticated local Docker CLI with **Docker Hub**.
- Learned **Image Tagging** conventions (`username/repository:tag`).
- Pushed a versioned image (`v1`) to a public registry.

### Public Image Link:
`https://hub.docker.com/r/<username>/devops-webserver`